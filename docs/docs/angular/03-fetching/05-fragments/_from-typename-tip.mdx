:::tip
In the example above, `__typename` can be omitted (e.g. `from: { id: '1' }`)  
**Orbit** will automatically extract the type name from the first fragment definition it encounters in the document, even if it contains multiple fragments.
:::
